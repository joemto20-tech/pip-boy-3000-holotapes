{
  "name": "big-iron-render-dock",
  "version": "2.0.0",
  "private": true,
  "description": "B.I.R.D. 2 - memory-aware Big Iron world, interior, NPC, dialogue, and asset editor.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node scripts/build.js && node server.js --check",
    "build": "node scripts/build.js"
  }
}
